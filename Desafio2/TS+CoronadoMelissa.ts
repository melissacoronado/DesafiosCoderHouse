import { Operaciones } from "./operaciones"
  
function operacion(num1: number, num2: number, operacion: string) {
    let ejecutar = new Operaciones(1, 1, 'suma');
    /*switch (operacion){
        case "suma":{ return num1 + num2; }
        case "resta":{ return num1 - num2; }
        
    }*/
}